# 🍔 API и веб-интерфейс для информации о калорийности бургеров

## 🌐 Обзор
Этот проект предоставляет полноценное веб-приложение для получения информации о калорийности и питательной ценности бургеров.

1. **Парсинг** парсинг данных с сайта вкусно и точка 
2. **postgresSQL** внесение данных в базу 
2. **Бэкенд API** на FastAPI - предоставляет REST API для доступа к данным о бургерах
3. **Фронтенд веб-интерфейс** - современный интерфейс для поиска и отображения информации о бургерах

Приложение позволяет:
- Просматривать полный список бургеров с их характеристиками
- Искать конкретный бургер по названию
- Получать детальную информацию о калорийности, белках, жирах и углеводах
- Просматривать фотографии бургеров

## 📋 Оглавление
- [Требования](#️-требования)
- [Структура проекта](#-структура-проекта)
- [Установка и запуск](#️-установка-и-запуск)
- [API Эндпоинты](#-api-эндпоинты)
- [Фронтенд возможности](#-фронтенд-возможности)
- [База данных](#-база-данных)

---

## 📌 Требования

### Бэкенд зависимости:
- FastAPI==0.104.1
- SQLAlchemy==2.0.23
- Pydantic==2.5.0
- Uvicorn==0.24.0
- psycopg2-binary==2.9.9
- python-dotenv==1.0.0
- asyncpg==0.29.0

### Фронтенд:
- Modern browser with JavaScript enabled
- No additional installation required (pure HTML/CSS/JS)


## ⚙️ Установка и запуск

1. **Клонирование репозитория**:
   ```bash
        git@github.com:askerovasif19933-stack/asinc_alchemy.git    
   cd test_task_async_sqlalchemy
   ```

2. **Настройка переменных окружения: создайте файл .env на основе [.env.example](./.env.example) и заполните его  данными для подключения к базе данных.**


git clone <ваш-репозиторий>
cd burger-calories-app
2. Настройка бэкенда
bash
cd backend
pip install -r requirements.txt
3. Настройка базы данных
Создайте файл .env в папке backend на основе .env.example:

env
DB_HOST=localhost
DB_PORT=5432
DB_NAME=burger_calories
DB_USER=postgres
DB_PASSWORD=your_password
DATABASE_URL=postgresql://postgres:your_password@localhost:5432/burger_calories
4. Создание базы данных
Создайте базу данных burger_calories в PostgreSQL и выполните миграции (если есть файл миграций).

5. Заполнение данными
Добавьте данные о бургерах в таблицу products с полями:

name - название бургера

weight_g - вес в граммах

calories_kkl - калории в ккал

nutritional_value_kj - энергетическая ценность в кДж

fats_g - жиры в граммах

carbohydrates_g - углеводы в граммах

protein_g - белки в граммах

image - URL ссылка на фото бургера

6. Запуск бэкенда
bash
cd backend
uvicorn main:app --reload --host 0.0.0.0 --port 8000
Бэкенд будет доступен по адресу: http://localhost:8000
Документация API (Swagger): http://localhost:8000/docs

7. Запуск фронтенда
Откройте файл frontend/index.html в браузере или используйте Live Server в VS Code.

🚀 API Эндпоинты
GET /
Описание: Корневой эндпоинт, возвращает приветственное сообщение
Пример ответа:

json
{
  "message": "Добро пожаловать! Перейдите на /docs для документации"
}
GET /burgers/
Описание: Получить список всех бургеров
Ответ: List[Burger]
Пример запроса:

bash
curl http://localhost:8000/burgers/
GET /burgers/search
Описание: Поиск бургера по названию (регистронезависимый поиск)
Параметры: name - название или часть названия бургера
Пример запроса:

bash
curl "http://localhost:8000/burgers/search?name=Чизбургер"
Модель ответа (Burger):

json
{
  "image": "https://example.com/burger.jpg",
  "name": "Чизбургер",
  "weight_g": 200,
  "calories_kkl": 550,
  "nutritional_value_kj": 2300,
  "fats_g": 30,
  "carbohydrates_g": 45,
  "protein_g": 25
}
💻 Фронтенд возможности
Основные функции:

Поиск бургеров - мгновенный поиск по названию

Показать все - отображение полного каталога бургеров

Адаптивный дизайн - корректное отображение на мобильных устройствах

Визуальные карточки - привлекательное представление информации

Структура интерфейса:

Заголовок - название приложения и описание

Панель поиска - поле ввода и кнопка поиска

Кнопка "Показать все" - загружает все доступные бургеры

Сетка карточек - отображение результатов поиска

Футер - информация о проекте

Карточка бургера включает:

📸 Фотография бургера (из базы данных)

🏷️ Название и вес

🔥 Калорийность (ккал и кДж)

📊 Пищевая ценность (белки, жиры, углеводы)

🗄️ База данных

Структура таблицы products:

sql

CREATE TABLE products (
    id SERIAL PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    weight_g INTEGER NOT NULL,
    calories_kkl INTEGER NOT NULL,
    nutritional_value_kj INTEGER NOT NULL,
    fats_g INTEGER NOT NULL,
    carbohydrates_g INTEGER NOT NULL,
    protein_g INTEGER NOT NULL,
    image VARCHAR(500),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP

);



Рекомендуемые индексы:

sql
CREATE INDEX idx_products_name ON products USING gin(name gin_trgm_ops);
CREATE INDEX idx_products_calories ON products(calories_kkl);

Пример данных:

sql

INSERT INTO products (name, weight_g, calories_kkl, nutritional_value_kj, fats_g, carbohydrates_g, protein_g, image) VALUES
('Чизбургер', 200, 550, 2300, 30, 45, 25, 'https://example.com/cheeseburger.jpg'),
('Гамбургер', 180, 450, 1880, 20, 40, 20, 'https://example.com/hamburger.jpg'),
('Биг Мак', 300, 750, 3140, 45, 60, 35, 'https://example.com/bigmac.jpg');


🔧 Технические особенности

Бэкенд:

FastAPI - высокопроизводительный фреймворк для Python

SQLAlchemy - ORM для работы с базой данных

Pydantic - валидация и сериализация данных

CORS Middleware - разрешены запросы со всех доменов

Асинхронные эндпоинты - эффективная обработка запросов

Фронтенд:
Чистый HTML/CSS/JavaScript - без зависимостей

CSS Grid и Flexbox - современная верстка

CSS анимации - плавные переходы

Fetch API - асинхронные запросы к бэкенду

Обработка ошибок - корректная работа при недоступности сервера

Безопасность:
✅ Валидация входных данных

✅ Защита от XSS через безопасный рендеринг

✅ CORS настройки для API

✅ Проверка URL изображений

🐳 Docker поддержка (опционально)

Для развертывания через Docker создайте docker-compose.yml:

yaml
version: '3.8'

services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: burger_calories
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: your_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql://postgres:your_password@postgres:5432/burger_calories
    depends_on:
      - postgres

volumes:

  postgres_data:

🚀 Быстрый старт


Установите PostgreSQL и создайте базу burger_calories

Запустите бэкенд: uvicorn main:app --reload

Откройте frontend/index.html в браузере

Начните поиск бургеров!

📞 Поддержка
При возникновении проблем:

Проверьте, что PostgreSQL запущен

Убедитесь, что бэкенд доступен по http://localhost:8000

Проверьте консоль браузера (F12) на наличие ошибок

Убедитесь, что в базе данных есть записи в таблице products

📄 Лицензия


MIT License - свободное использование и модификация.
