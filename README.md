




images/burger.png








# 🍔 **API и веб-интерфейс для информации о калорийности бургеров**

---

## 🌐 **Обзор проекта**

Этот проект — полноценное **веб-приложение** для получения информации о **калорийности и пищевой ценности бургеров** сети **«Вкусно — и точка»**.

### Проект включает:

* 📦 **Готовый набор данных (JSON)** — используется для наполнения базы данных
* 🗄️ **PostgreSQL** — хранение информации о бургерах
* ⚙️ **Backend API (FastAPI)** — REST API для доступа к данным
* 💻 **Frontend (HTML / CSS / JavaScript)** — веб-интерфейс для поиска и просмотра

> 🔎 **Парсер** сохранён в репозитории в учебных целях,
> но **в работе используется уже готовый JSON-файл**.

---

## ✨ **Возможности приложения**

* 🔍 Просмотр полного списка бургеров
* 🏷️ Поиск бургера по названию (регистронезависимый)
* 🔥 Получение данных о:

  * калорийности
  * белках, жирах и углеводах
  * весе продукта
* 🖼️ Отображение изображений бургеров
* 📘 Swagger-документация API

---

## 📁 **Структура проекта**

```text
project/
├── docker-compose.yml
├── Dockerfile
├── requirements.txt
├── .env.example
├── .env
│
├── src/
│   ├── main.py          # FastAPI приложение
│   ├── database.py
│   ├── models.py
│
├── static/
│   └── front.html       # HTML-интерфейс
│
├── pars/
│   └── parser.py        # Парсер (не используется при запуске)
│
├── data/
│   └── burgers.json     # Готовые данные
│
└── README.md
```

---

## 📌 **Требования**

Для запуска проекта **ничего, кроме Docker, не требуется**:

* 🐳 **Docker** — [https://www.docker.com/get-started](https://www.docker.com/get-started)
* 🧩 **Docker Compose** — [https://docs.docker.com/compose/](https://docs.docker.com/compose/)

---

## 🚀 **Быстрый старт (одна команда)**

### 1️⃣ **Клонировать репозиторий**

```bash
git clone git@github.com:askerovasif19933-stack/calorie-content-of-burgers.git
cd calorie-content-of-burgers
```

---

### 2️⃣ **Создать файл окружения**

```bash
cp .env.example .env
```

Заполните данные подключения к базе данных:

```env
DB_HOST=db
DB_PORT=5432
DB_NAME=burger_calories
DB_USER=postgres
DB_PASSWORD=postgres
DATABASE_URL=postgresql://postgres:postgres@db:5432/burger_calories
```

---

### 3️⃣ **Запустить проект**

```bash
docker compose up --build
```

❗ **Рекомендуется запускать без `-d`**, чтобы видеть логи приложения.

---

### 4️⃣ **Открыть в браузере**

| 🔗 Назначение | 🌍 Адрес                                                                           |
| ------------- | ---------------------------------------------------------------------------------- |
| Backend API   | [http://localhost:8000](http://localhost:8000)                                     |
| Swagger UI    | [http://localhost:8000/docs](http://localhost:8000/docs)                           |
| Frontend      | [http://localhost:8000/static/front.html](http://localhost:8000/static/front.html) |

---

## 🔌 **API эндпоинты**

### 📥 **Получить список всех бургеров**

```http
GET /burgers/
```

```bash
curl http://localhost:8000/burgers/
```

---

### 🔍 **Поиск бургера по названию**

```http
GET /burgers/search?name=Чизбургер
```

```bash
curl "http://localhost:8000/burgers/search?name=Чизбургер"
```

---

### 📦 **Пример ответа API**

```json
{
  "image": "https://example.com/burger.jpg",
  "name": "Чизбургер",
  "weight_g": 200,
  "calories_kkl": 550,
  "nutritional_value_kj": 2300,
  "fats_g": 30,
  "carbohydrates_g": 45,
  "protein_g": 25
}
```

---

## 💻 **Frontend**

### Основные возможности:

* 🔎 Поиск бургеров
* 📋 Просмотр всех позиций
* 🖼️ Карточки с изображениями
* 📱 Адаптивная верстка

### Используемые технологии:

* **HTML5**
* **CSS (Grid + Flexbox)**
* **Vanilla JavaScript**
* **Fetch API**

---

## 🗄️ **База данных**

### 📦 Таблица `products`

```sql
CREATE TABLE products (
    id SERIAL PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    weight_g INTEGER NOT NULL,
    calories_kkl INTEGER NOT NULL,
    nutritional_value_kj INTEGER NOT NULL,
    fats_g INTEGER NOT NULL,
    carbohydrates_g INTEGER NOT NULL,
    protein_g INTEGER NOT NULL,
    image VARCHAR(500),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### ⚡ Рекомендуемые индексы

```sql
CREATE INDEX idx_products_name ON products USING gin(name gin_trgm_ops);
CREATE INDEX idx_products_calories ON products(calories_kkl);
```

---

## 🛠 **Технические особенности**

### Backend:

* ⚙️ **FastAPI**
* 🧠 **SQLAlchemy 2.0**
* 📦 **Pydantic**
* 🔁 **Alembic (миграции)**
* 🌐 **CORS Middleware**

### Frontend:

* 🚫 Без фреймворков
* ⚡ Асинхронные запросы
* ❗ Обработка ошибок

---

## 🧪 **Парсер**

Парсер расположен в папке **`pars/`** и **не запускается автоматически**.
Он сохранён исключительно в учебных целях.

---

## 📄 **Лицензия**

**MIT License** — свободное использование и модификация.

---

## ✅ **Итог**

* ✔ Запуск одной командой
* ✔ Docker-first подход
* ✔ Чистая структура
* ✔ Подходит для учебного проекта и портфолио

---

📌 **Проект готов к использованию и публикации на GitHub.**
